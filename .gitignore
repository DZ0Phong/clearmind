# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# CLI artifacts
cli/node_modules
cli/assets/icon.png
cli/assets/icon.ico
cli/package-lock.json
# Auto-generated by url-scheme.js at first CLI boot per install — paths
# are baked in so it's installation-specific, not source.
cli/url-handler-launcher.vbs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.test/

# Live-view CDP driver + its screenshots — local dev tooling (see NOTES.md)
.cm-*

# Tauri / Rust build output (generated; Cargo.lock IS committed when present)
src-tauri/target/
src-tauri/gen/
# Intermediate icon source — regenerated from cli/icon.js via `tauri icon`
src-tauri/app-icon.png
# Updater signing PRIVATE key — NEVER commit; lives only as a GitHub Actions secret
src-tauri/.updater-key*
